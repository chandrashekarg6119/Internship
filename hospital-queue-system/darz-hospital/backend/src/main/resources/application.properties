spring.application.name=darz-hospital

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/darz_hospital?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Gcs@191204
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.secret=darzHospitalSecretKey2024VeryLongSecretKeyForJWTTokenGenerationAndValidation
app.jwt.expiration=86400000

# Server
server.port=8080

# CORS
app.cors.allowed-origins=http://localhost:3000

# Logging
logging.level.com.darz.hospital=DEBUG
